# yaml-language-server: $schema=https://raw.githubusercontent.com/deepmap/oapi-codegen/HEAD/configuration-schema.json
package: hvacunits
output: hvacunits/hvacunits.gen.go
generate:
  models: true
  client: true